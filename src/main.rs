//! ccoptimizer CLI entry point.
//!
//! The `analyze` / `report` commands (see `docs/specs/cli.md`) are not wired up
//! yet — they are built under their own contracts. For now this is a placeholder
//! so the binary target compiles alongside the library.

fn main() {
    eprintln!("ccoptimizer: not implemented yet — see docs/specs/cli.md");
    std::process::exit(1);
}
